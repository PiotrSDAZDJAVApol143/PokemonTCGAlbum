# ---- BAZA DANYCH ----
spring.datasource.url=jdbc:mysql://localhost:3306/pokemontcg?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=SDA143pk!

# ---- JPA / HIBERNATE ----
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ---- Liquibase ----
spring.liquibase.change-log=classpath:db/liquibase-changelog.yaml

# ---- PORT ----
#server.port=8080
